services:
  frontend:
    container_name: sportevents-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    networks:
      - sportevents-network

networks:
  sportevents-network:
    external: true
