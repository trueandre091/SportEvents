import * as React from 'react';
import { Box, Typography } from '@mui/material';
import Header from '../components/Header';

const Events = () => {
  return (
    <Box>
      <Header />
      <Typography variant="h4">Все мероприятия</Typography>
    </Box>
  );
};

export default Events;